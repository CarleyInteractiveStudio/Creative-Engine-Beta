// Script para probar el nuevo sistema de componentes

go "CallbackTest"

public star() {
    console.log("Iniciando prueba desde test_callbacks.ces...");
    test(); // Llamar a la función de la librería
}

public update(deltaTime) {
    // No se necesita nada aquí para esta prueba
}
